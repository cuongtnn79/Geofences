apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'BrianTranAndroidKey'
            keyPassword '123159*ncbl'
            storeFile file('C:/Users/Nam Cuong/keystores/android.jks')
            storePassword '123159*llbl'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.0"
    defaultConfig {
        applicationId "com.namcuong.genfences"
        minSdkVersion 21
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
        debug {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}
